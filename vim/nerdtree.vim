" NerdTree overrides the i key, so we need to remap this key to be able
" to navigate using our customized arrow keys. since both

" default 'i'
let NERDTreeMapOpenSplit='s'
" default 's'
let NERTTreeMapOpenVSplit='v'
